# 广财工作台 上线操作手册（傻瓜式·一步步点）

目标：把工作台部署到对外可访问网址 **https://gongcai.wegrow2025.com**
当前状态：代码已推上 GitHub（仓库 `abdullahlily641-jpg/-guangcai-workbench`，文件齐全），只需做以下 3 件事即可上线。

---

## 准备：打开你的仓库页面
1. 浏览器打开 **https://github.com/abdullahlily641-jpg/-guangcai-workbench**
2. 确认你能看到 `index.html` `CNAME` `.nojekyll` `README.md` 四个文件（说明代码在，正常）。

---

## 第 1 步：开启 GitHub Pages（让代码变成网页）

1. 在仓库页面顶部，点最右侧的 **「Settings」**（齿轮图标，不是仓库列表里的设置）。
2. 左侧菜单往下滚，找到 **「Pages」**（在 "Code and automation" 分组下），点它。
3. 在 **「Build and deployment」** 这一块：
   - **Source（来源）** 下拉框，选 **「Deploy from a branch」**。
   - **Branch（分支）** 下拉框，选 **`main`**。
   - 右边 **Folder（目录）** 下拉框，选 **`/ (root)`**。
4. 点 **「Save」** 按钮。
5. 页面会刷新，上方出现一行绿条 "GitHub Pages is now building your site… **✅**"。
   - ⏱ 等 1～2 分钟，刷新本页，顶部会显示：
     **"Your site is live at https://abdullahlily641-jpg.github.io/-guangcai-workbench/"**
   - 把这个地址复制到浏览器，能打开工作台 = 第 1 步成功。

---

## 第 2 步：去 wegrow2025.com 的 DNS 后台加一条解析（让域名指向 GitHub）

> 这一步在「管理 wegrow2025.com 域名」的地方做（一般是 腾讯云 DNSPod / 阿里云，哪里买的域名去哪里）。
> 建议**先做完第 2 步，再回 GitHub 做第 3 步**，这样第 3 步的验证能一次过。

1. 登录你管理 `wegrow2025.com` 的域名后台 → 进入 **解析 / DNS 管理** → 找 **`wegrow2025.com`** 这个域名。
2. 点 **「添加记录」**（或「新手引导 / 添加解析」）。
3. 按下表**一字不差**填写：

   | 项目 | 填什么 |
   |------|--------|
   | 记录类型 | **CNAME** |
   | 主机记录（前缀/子域名） | **gongcai** |
   | 记录值 / 目标地址 | **abdullahlily641-jpg.github.io** |
   | 线路类型 | 默认（不用改） |
   | TTL | **600**（或 10 分钟，随便） |

   - ⚠️ 记录值**不要**带 `https://`，也**不要**带末尾的 `.`（有些后台会自动补，没关系）。
   - ⚠️ 主机记录只填 `gongcai`，**不要**填成 `gongcai.wegrow2025.com`（后台会自动拼后缀）。
4. 点 **「保存 / 确认」**。出现一条新记录 = 第 2 步成功。

### 阿里云（万网）后台具体操作
1. 打开 **https://home.console.aliyun.com** 登录 → 搜 **「云解析DNS」**（或「产品与服务 → 域名 → 域名解析」）。
2. 域名列表找 **`wegrow2025.com`** → 点 **「解析设置」**。
3. 在解析记录列表里找**主机记录 = `gongcai`** 的那条（现在是 A 记录）：
   - 有 → 点右边 **「修改」**；没有 → 点右上 **「添加记录」**。
4. 填写：记录类型 **CNAME** ｜ 主机记录 **gongcai** ｜ 解析线路 默认 ｜ 记录值 **abdullahlily641-jpg.github.io** ｜ TTL 10 分钟 → **确认**。
5. 改完回 GitHub Pages 页面点黄色框 **「Check again」**，等变绿 `DNS check successful`。

---

## 第 3 步：回 GitHub 绑定自定义域名 + 开启 HTTPS

1. 回到第 1 步那个 **Settings → Pages** 页面。
2. 往下找 **「Custom domain」**（自定义域名）输入框。
3. 填入：**`gongcai.wegrow2025.com`**（和你第 2 步的主机记录一致）。
4. 点 **「Save」**。
5. 页面会显示一条提示 "A record was created…" 或 "DNS check in progress"，**不用管，等它自己变**。
   - 过 1～5 分钟刷新本页：当提示变成 **"✅ DNS check successful"** 时，代表域名解析已通。
6. 这时 **「Enforce HTTPS」** 复选框会从灰色变可选 → **立刻打勾 ✅**。
   - 若还是灰色：说明 SSL 证书还在签发（最长 24 小时，通常几分钟），**过会儿回来刷新再勾**即可。

---

## 第 4 步：验证上线成功

1. 浏览器打开 **https://gongcai.wegrow2025.com**（注意是 https）。
2. 能看到蓝色「广财财税AI营销项目 · 员工执行分工工作台」= 🎉 全部完成！
   - 第一次可能要等 DNS 全球生效（几分钟～几小时）。生效前访问会显示"无法访问/不安全"，属正常，稍后再试。
   - 想立刻验证代码没问题，可先用第 1 步的 `https://abdullahlily641-jpg.github.io/-guangcai-workbench/` 地址确认。

---

## 以后改了内容怎么重新发布（常用）

工作台 `index.html` 改完想更新到线上，只需 2 条命令（在你电脑的 `github-pages-deploy` 文件夹里右键"Git Bash Here"）：

```bash
git add -A
git commit -m "更新工作台"
git push
```

> 提示：之前那枚 `ghp_...` 的 classic token 仍有效、已配好，直接 `git push` 会弹窗让你登录一次；登录后 GitHub 会自动重新部署，通常 1 分钟内线上更新。
> 安全建议：上线完成后，去 GitHub → Settings → Developer settings → Personal access tokens → 把该 token 设为过期/删除。

---

## 常见问题速查

| 现象 | 原因/处理 |
|------|-----------|
| 第 3 步一直 "DNS check unsuccessful" | 第 2 步 CNAME 记录值写错（应是 `abdullahlily641-jpg.github.io`，不是仓库地址）；或 DNS 还没生效，等一会儿再刷新 |
| 打开 gongcai.wegrow2025.com 显示"不安全/证书错误" | Enforce HTTPS 还没勾 / 证书未签发完，回到第 3 步第 6 点等几分钟再勾 |
| 打开网址是 404 | Pages 没 Save 成功，或 Branch 没选 `main`、Folder 没选 `/ (root)`，重做第 1 步 |
| 想换成别的子域名（如 workbench.wegrow2025.com） | 改 `CNAME` 文件内容为 `workbench.wegrow2025.com` + 第 2 步主机记录改成 `workbench` + 第 3 步填新域名，重新推送 |
